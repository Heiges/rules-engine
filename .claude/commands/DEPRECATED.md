# Deprecated

Dieser Ordner wird nicht mehr aktiv gepflegt.

Der bisherige Ansatz (Implementierung und Dokumentation in einem Schritt) wird durch den blueprint-first Workflow abgelöst: Spezifikation in `.claude/appspecs/`, Implementierung auf Basis der Spec.

Ausnahme: `review.md` bleibt weiterhin gültig.
